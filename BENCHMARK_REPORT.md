# Cortex Benchmark Evidence Report

## Current release surface

| Field | Value |
|---|---|
| Cortex version | 9.8.2 |
| Runtime commit | `ca26e1d6fbda7b3323e875c7fba8ccfc12ca972d` |
| Benchmark manifest | [`benchmarks/results/MANIFEST.json`](benchmarks/results/MANIFEST.json) |
| Evidence phase | [`v9.8.2 Information-Balanced Held-Out Seal`](docs/intelligence/PHASE_V9.8.2_INFORMATION_BALANCED_HELDOUT_SEAL.md) |
| Prior independent audit date | August 24, 2026 |
| Exact v9.7 audited HEAD | `797e06f2ab860bba7f13888a3213ccc9aa863e7d` |
| Evidence class | Mixed: controlled synthetic, structural, and one prior live empirical circulation |
| Supersedes | Historical root v3.1 report, retained in Git history |

## Executive boundary

Cortex has verified mechanisms for bounded context, canonical provenance,
provider-neutral model circulation, independent evaluation, governance, and
controlled competence experiments. One real provider-backed model boundary was
crossed before v9.8.

The first live A–E competence cohort produced success in every arm (`n=1`) and
therefore zero measured lift. Cortex has **not** demonstrated that distilled
competence improves a model. v9.8 preserves that null and adds semantic-support
and preregistered causal-evidence gates for the next experiment. v9.8.1 also
measures whether an experiment contains enough information to reveal an effect.

## v9.8.1 fresh measurement findings

| Measurement | Fresh result | Interpretation |
|---|---|---|
| Runtime-selected frontier calibration | 8/8 exact tasks | Ceiling; development-only; not confirmatory |
| Thalamus rerun | rank 1 baseline; rank 1 routed | Zero ranking contrast |
| Deferred/eager rerun | 0.394 s deferred; 0.555 s eager | 29% lower bootstrap median; lifecycle cost unresolved |
| Synthetic foreign-host rerun | 5/5 declared host shapes passed | Controlled synthetic only |
| Self-host rerun | 2 runs/arm completed | Heavy controlled workload; not an interactive benchmark |

The frontier calibration persisted only public structured answers and bounded
invocation metadata. Its executable and model were runtime-selected. Hidden
reasoning and the raw provider envelope were not persisted. At 100% success,
Bernoulli entropy is `H(Y)=0`; this diagnoses an easy task panel rather than
demonstrating competence lift.

The deferred benchmark now gives every timing sample a fresh host and fresh
Cortex home. This preserves the home-identity guard and removes a benchmark
harness confound. Thirty deferred materialization operations remain, so the
result is not evidence of lower total lifecycle work.

## v9.8.2 experiment-design result

The model-neutral forge generated an 80-case development ladder:

```text
5 task families × 4 difficulty levels × 4 variants = 80 cases
development corpus hash = eb1d50acaddb4287c6d7964dea2d3226587ef98d5f8593be3c00ea6d9431c9bb
frontier calibration = NOT EXECUTED
held-out corpus = NOT GENERATED
confirmatory effect = NOT ESTABLISHED
```

The artifact verifies deterministic task construction and the persistent
test/production distinction. It is structural evidence only. A held-out corpus
will not be generated until real development outcomes identify an informative
difficulty level for every retained family.

The first adversarial held-out test discovered that some nominally reseeded
tasks remained semantically identical. v9.8.2 now varies actual task parameters
across every family and rejects any development/held-out case overlap.

## Current committed quantitative artifacts

The current results directory contains four primary JSON result sets. Their
exact hashes and sizes are recorded in the machine-readable manifest.

| Benchmark | Current finding | Evidence class |
|---|---|---|
| Thalamus routing | Target rank 4 → 3; Recall@3 0 → 1; latency +1.41%; intrusion unchanged | Controlled synthetic |
| Deferred/eager | Bootstrap 283.120 ms → 211.294 ms deferred (25.37% reduction) | Controlled synthetic |
| Foreign host matrix | 5/5 generated host shapes passed declared certificate/mutation gates | Controlled synthetic |
| Self-host | Nested bootstrap +7.82%; activation +3.27% | Controlled workload |

These artifacts predate the unified v9.8 manifest. The manifest labels their
missing per-run metadata `legacy_partial`; hashing them now does not constitute
a rerun.

## Live empirical competence result

The preserved v9.6 live cohort remains:

```text
A ordinary context        1/1
B raw public history      1/1
C admitted memory         1/1
D distilled competence    1/1
E competence + feedback   1/1
```

All primary gains equal `0.0`. v9.7 correctly classified this cohort as held
because it has one case, a ceiling baseline, and zero dynamic range. v9.8 adds
no new empirical observation and makes no positive causal claim.

## v9.8-v9.8.2 measurement changes

- New competence semantic identities bind operational material beside IDs.
- Immutable semantic witnesses distinguish exact canonical support from
  unsupported/unknown generalization.
- Confirmatory trials require a frozen pre-execution design.
- Primary binary effects use exact matched-pair discordance tests with Holm
  multiplicity correction.
- A one-case panel reports variance as not estimable.
- Fixture evidence remains structural and cannot satisfy empirical promotion.
- Calibration reports Bernoulli entropy and rejects floor, ceiling,
  undersampled, and zero-discordance panels.
- Exact power planning replaces arbitrary confirmatory sample counts.
- A deterministic five-family task forge is development-only by construction.
- Readiness composes as `Theta=min(E,D,I)`; continuous scores cannot compensate
  for missing semantic evidence, discriminability, or replication.
- Rasch-style difficulty estimates and `I=p(1-p)` select an informative
  development level without using model/provider identity.
- The answer-free held-out public manifest binds to a private answer-key
  commitment and its exact development calibration.

## Still unresolved

- positive causal competence lift;
- semantic prerequisite/counterevidence completeness;
- powered multi-task, multi-model confirmatory evidence;
- production-repository portability and scaling;
- decomposed database/storage overhead;
- cold/warm lifecycle costs;
- external independent replication.

The historical v3.1 benchmark narrative is archival and remains accessible in
Git history. It is no longer presented as the current release report.
