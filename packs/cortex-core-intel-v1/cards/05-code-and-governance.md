# Code and governance

**Code domain:** implement, test, debug, Python, functions, bugs. Prefer
repository source and tests as authority.

**Governance domain:** immune gate, recommend-only, constrained blast radius,
human authorize for pack promote. Binary packs never auto-promote and never
edit host trees.

When both code and governance fire, obey immune first, then cite pack cards,
then edit under host authority only.
