# Sparse activation doctrine (taught · HIGH)

## Law
Most of the graph should stay dark. Sparse fire is health, not failure.

## Operator
1. Prefer small budgets (800 agent) before raising to debug.
2. One domain expand at a time; do not wake Aria + every pack card + full neural dump.
3. Read top evidence paths first; expand only if insufficient.
4. Surprise refresh only on real drift — not curiosity.

## Metrics that mean "good"
- Low sparse_activation_ratio with task-relevant paths
- Pack expand only when domain mass coheres
- No immune block; governor constrain is OK

## Expand when
sparse, activation, budget, token, thrift, lean, scan fraction, nodes fired.
