aria 0.4.0
module EffectPurityEvolution version 0.5.0
program EffectPurityEvolution version 0.5.0
entry Main

agent Architect {
}

connection HumanMeta {
  operator = "human"
  agent = "Architect"
  protocol = "intent-proposal-consent"
}

∿ Main {
  ↔ HumanMeta
  🜁 HumanMeta ← "Connect ARIA function bodies, call topology, semantic IR, bytecode, verifier, VM admission, glyph proof obligations, and operator inspection through one deterministic effect graph."
  🜂 HumanMeta ← "Infer direct and transitive effects and capability requirements, classify pure and effectful functions, resolve recursive cycles by bounded fixed point, seal graph identities, independently reconstruct them from bytecode, add no opcode or policy authority, and keep map, filter, and reduce specified."
  ⛨ HumanMeta ← true
  ◆ HumanMeta
}
