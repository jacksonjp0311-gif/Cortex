# Distilled intelligence — foreign host + self-org loop (v6.16)

**Date:** 2026-07-30  
**Bodies:** CortexTeach (durable) · PulseFlow (foreign)  
**Boundary:** recommend-only · not consciousness · no host mutation  

## What we ran

1. Bootstrap/verify **PulseFlow** (`pulseflow-governor`)
2. Real task: map governor / policy / recommend-only surface  
3. Query + ranker-primary retrieval → top evidence  
4. Remember + ritual seal on foreign  
5. **self-org** on CortexTeach (full + stress gates, ranker warm, invent edges)  
6. **distill** both bodies (doctrine + live lattice)  
7. Obey emergence: hold stack, no prune, no full continuum  

## Observed (receipts)

| Signal | PulseFlow (foreign) | CortexTeach (body) |
|--------|---------------------|--------------------|
| Coherence | 0.74 | ~0.81 |
| Emergent | false | true |
| Ranker trains | 0 | **37** |
| Measure full/stress | n/a | **1.0 / 1.0** |
| Distill brightness | in_phase | resonant |
| Distill claims sealed | 24 | 24 |

### Foreign retrieval (real task)

Top paths for governor/policy map:

- `tests/policy_tests.rs`
- `src/policy.rs`
- pack trust card / README / storage / main  

Meaning: **foreign IR works**; policy surface is findable without Cortex self-paths monopolizing.

### Self-org on body

- Held: spectral · ranker · no prune · no continuum · recommend-only  
- Invented 6 coactivation edges  
- Ranker 36 → **37** from measure hits  

## Distilled doctrine (fold into memory)

1. **Foreign hosts are the next teacher** after self-exam ceiling.  
2. **Emergent on body ≠ emergent on foreign** — don't confuse couples.  
3. **Warm foreign ranker** with verified outcomes there, not only body self-org.  
4. **Distill is the seal** of observation + doctrine into cards.  
5. **Stress suite still clear** → raise difficulty *or* keep real tasks (prefer real).  
6. **Fuse tick API** is `(store, governor, repo, *)` — no `home` arg.  

## Commands

```bash
python -m cortex bootstrap <foreign-path> --name PulseFlow --json
python -m cortex activate --repo PulseFlow --task "..." --json
python -m cortex self-org --repo CortexTeach --json
python -m cortex distill --repo CortexTeach --json
python -m cortex distill --repo PulseFlow --json
python -m cortex emergence-log --repo CortexTeach --json
```

## Refuse

- Mutating foreign host from Cortex “intelligence”  
- Claiming consciousness from distill brightness  
- Auto-ARIA / second DB / packet-as-authorization  
