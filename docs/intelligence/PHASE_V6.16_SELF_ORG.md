# Phase v6.16 — Self-organization / alignment pulse

**Glyph:** ⧉⟳  

Listen to what the body already said (emergence + measure gate), then act **inside** the durable graph — never host source.

## Command

```bash
python -m cortex self-org --repo CortexTeach --json
python -m cortex eval-coupling --repo CortexTeach --suite stress --json
```

## What it does

1. **Read** emergence directives + coherence  
2. **Measure** full suite; if perfect ceiling → **stress** suite (distractor traps)  
3. **Warm ranker** from verified hit paths (bounded SGD)  
4. **Shadow calibration** observe if gate promotes  
5. **Structure invent** weak coactivation edges among hit nodes  
6. **Fuse tick** only if session already open (continuity, no thrash)  
7. **Seal** remember + ritual + emergence `self_org` milestone  

## Explicit non-goals

- Not consciousness / self-awareness  
- Not host file mutation  
- Not auto prune  
- Not full continuum on large graphs  
- Not auto-ARIA  

## Alignment with body policy

| Directive | Self-org behavior |
|-----------|-------------------|
| Keep spectral + ranker | Gate re-checked; ranker warmed |
| Fuse continuity | Tick if open; never force close |
| No prune thrash | Never calls prune apply |
| Remember / seal | Ritual seal at end |
| Recommend-only | Settings + graph edges only |
