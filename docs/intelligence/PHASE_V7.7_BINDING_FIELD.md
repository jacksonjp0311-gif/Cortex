# Phase v7.7.0 — Binding Field

**Tagline:** Name the gap between local coupling and constitutional readiness — then close the buffer without silent authority.

## What interconnect said (and we agreed)

Live Cortex body (v7.6 era measurement):

| Signal | Reading | Meaning |
|--------|---------|---------|
| Self-sense | **UNBOUND** | Epoch/phase lag — never false healthy |
| Field frames | **0/16** | Temporal baseline cold |
| Live buffer | **~11 samples** | Activity without frame close |
| Warm-in ready | **false** | Milestone not met |
| Sealed version | lagged live engine | Continuity realign needed |
| Immune | mutation held | Expected recommend-only |

**Structure:** local spectral/graph coupling can be alive while global binding and temporal frame closure lag.

## Binding Field

Composite advisory field:

```text
epoch/phase binding
live buffer depth
closed frame warmth
spectral integrate/retain mass
self-sensing class / residual
        ↓
BINDING_GAP | BUFFER_PENDING | COLD_FIELD | VERIFIED_REGIME | …
        ↓
advisory recommendations only
```

## Classifications (priority)

1. **BINDING_GAP** — needs realign / unbound (highest priority; live pattern)
2. **BUFFER_PENDING** — buffer ticks without closed frames (your 11-sample buffer)
3. **COLD_FIELD** — frames started but not warm
4. **VERIFIED_REGIME** — bound + warm + sense not UNBOUND/COLD
5. **INDETERMINATE**

## CLI

```bash
python -m cortex binding-field observe --repo MyProject
python -m cortex binding-field commit --repo MyProject   # frame close only
python -m cortex binding-field report --repo MyProject --json
```

`commit` closes the Resonant Frame buffer. It **never** seals a body epoch.

## Claim boundary

Binding Field explains readiness structure. It does not grant authority, mutate host, or claim consciousness.
