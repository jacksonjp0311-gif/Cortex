# Cortex v10.0.0-alpha.37 — Contract-Aligned Frontier Baseline

Alpha.37 asks a frontier model to cross the repaired alpha.36 instrument once.
It does not project memory or competence and therefore cannot establish Cortex
improvement.

## Frozen path

```text
canonical alpha.36 result
  -> aligned public requirements
  -> host-private mapped assertions
  -> runtime-selected live adapter
  -> four task-only structured edit intents
  -> deterministic patch compilation
  -> isolated executable evaluation
  -> canonical trajectory reconstruction
  -> baseline disposition
```

The preregistration binds the exact alignment-result hash, aligned-corpus hash,
executable-corpus hash, private-bundle hash, model and adapter provenance,
response contract, case order, no-tool scope, and four-call limit.

## Sequential rule

```text
0–1 / 4  -> screening floor; forge easier tasks
2 / 4    -> calibrated development baseline; freeze sham/relevant treatment
3–4 / 4  -> screening ceiling; forge harder tasks
mismatch -> interpretation held; do not infer difficulty
```

No retries or post-hoc evaluator changes are permitted. A later semantic trial
requires new authority even if the baseline is calibrated.

## Live result

The exact committed implementation was exercised with the runtime-selected
experiment configuration `OpenAI / gpt-5.6-sol`:

| Case | Intent compiled | Candidate result |
|---|---:|---:|
| bounded budget | yes | PASS |
| write invalidates cache | yes | PASS |
| normalized rotation | yes | PASS |
| owned register snapshot | yes | PASS |

```text
calls executed                  4 / 4
task successes                  4 / 4
structured transport failures  0
canonical reconstruction       PASS
screen state                    screening_ceiling
semantic treatment projected   no
```

Preregistration receipt:
`03c9149ff6cec73651a2f98ae3cb1f4d281a34492e6bdd236b6570742cd0490a`

Result receipt:
`477a22ebc65f1aea59fcc6fbdd1f606b81fe337c2e52766c0e3043dee04e4fb6`

The outcome is informative even though it is not the desired middle band: the
contract-alignment repair removed the alpha.35 interpretation confound, and the
frontier model saturated the resulting panel. No semantic-treatment calls were
opened.

## Claim boundary

This phase measures task-only repair performance for one runtime-selected model
on four development cases. It cannot establish a population success rate,
semantic transfer, general model improvement, autonomous self-improvement, or
execution authority. Provider and model identity are provenance only and are
not coded into Cortex ontology.
