# Causal Lineage

`lineage_artifacts` + `lineage_edges` tables. Adaptive artifacts record parents, origin memory ids, controller, receipt hash.

Queries: `ancestors_of`, `descendants_of`, `propagation_trace`, `lineage_integrity_check`.
