# Witness Chronology

1. `commit_manifest` — hashes only  
2. `verify_reveal` — match hashes + snapshot/commit  
3. `run_witness` — requires preexisting commitment + revealed cases  

No `commit_manifest` inside evaluation.
