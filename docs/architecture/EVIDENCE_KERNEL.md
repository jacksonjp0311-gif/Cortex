# Evidence Kernel

Trusted Simplex controller. Separate code path from `retrieval.query`.

**Allowed:** host source/test/doc lexical hits, structural edges, certificate/manifest.

**Forbidden:** ranker, spectral, concept routes, invent, fusion, HNSW, discovery cards, adaptive budgets.

See `cortex/evidence_kernel.py`.
