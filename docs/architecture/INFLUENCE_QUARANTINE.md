# Influence Quarantine

`influence_policy.active_exclusions` + `filter_memory_hits` remove quarantined/invalidated artifacts from advanced retrieval and training paths. Evidence Kernel remains host-evidence only.
