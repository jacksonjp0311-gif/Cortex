# GSO-IIb — metabolic assembly

September 7, 2026. Product remains `10.0.0a39`. Zero model calls.
Shadow only. Production effect remains false.

This pass interconnects existing governed components. It does not rewrite
historical source-improvement v1, live GSO, or the exhausted 5/8 cohort.

## Circulation

```
Context -> Variation -> Transduction -> Observation -> Assurance
    -> Selection -> Retention -> Assembly -> Context
```

Compute flows. Verified structure recycles. Failures become scoped constraints.
External authority stays outside the loop.

## What was demonstrated

A deterministic shadow cycle produced both `T1 != T0` and a changed candidate
ranking after a retained assembly prior, with `AuthorityLeakage = 0` and
`ProductionEffect = 0`. Identity change alone is no longer counted as behavioral
recursive closure.

Component composition now requires independent type and schema compatibility.
An absent or `NOT_TESTED` environment is `UNKNOWN` and holds the edge. Manifest
identity and bounded positive claim states are checked before acceptance.

Retention eligibility is derived through adjacent, hash-linked lifecycle
transitions. A caller cannot construct a new candidate directly at
`RETENTION_ELIGIBLE`. The zero-call cycle uses evidence explicitly labeled
`SYNTHETIC_ZERO_CALL_CONTROL`; it does not masquerade as empirical retention.

That is mechanical self-assembly influence, not useful self-improvement.

CI is not skipped. Shadow reuse may return `REUSE_ELIGIBLE` only when the
complete declared dependency fingerprint is unchanged. Ambiguity recomputes.

## Not established

Autonomy, general self-organization, retention utility `U_r`, heredity `G_i`,
OS sandboxing, production topology activation, or authenticity of lifecycle
evidence against an external receipt store. The lifecycle is structurally
reconstructable within declared controls; empirical retention remains unrun.
